# SleepBleCollector

用于一次性诊断 Sleep 监测带的本地 BLE 通路，不依赖厂家账号或云端接口。

应用会：

1. 扫描附近 BLE 广播并优先连接名称包含 `midea` 的设备；
2. 记录广播原始字节、设备信息、GATT 服务/特征 UUID 与属性；
3. 读取所有可读特征，并订阅通知/指示，记录每次返回的原始十六进制字节；
4. 将日志保存到应用专属 Documents 目录，便于通过 ADB 导出。

这只是协议取证工具，不会修改设备，也不代表已知晓数据字段含义。能否得到温度、动作、翻身或离床字段，必须以设备实际返回的特征和通知字节为准。
