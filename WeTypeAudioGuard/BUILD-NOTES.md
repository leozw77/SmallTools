# Build notes

WeTypeAudioGuard default GitHub Actions artifact is a compact framework-dependent Windows x64 build. It requires the .NET 8 Desktop Runtime to be installed locally and does not bundle the .NET runtime.
